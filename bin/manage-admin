#!/usr/bin/env bash

#exec "`dirname "${BASH_SOURCE[0]}"`/pex" python -m os2datascanner.projects.admin.manage "$@"

DIR="`dirname "${BASH_SOURCE[0]}"`"
cd "$DIR/../src"
. "../python-env/bin/activate"
exec python -m os2datascanner.projects.admin.manage "$@"
